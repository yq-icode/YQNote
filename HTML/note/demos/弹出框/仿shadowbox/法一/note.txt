【仿shadowbox】

■使用说明：



■浏览器兼容：
IE6/IE7/IE8/FF/Chrome

■注意点：
要想在IE6下也固定不动，必须通过在CSS中写JS来设置popbox-wrapper的TOP值，因此在IE6下必须避免用JS来设置TOP值，因为会覆盖CSS中的值；

■缺陷：
在IE6中有以下缺陷
当网页内容不满一屏时，阴影亦不能覆盖满屏，此时必须设置body{_height:100%;},设置之后虽然可以解决此问题，但是当弹出框出现时，不满一屏的网页出现了滚动条，
如果将网页上的内容都去掉，滚动条就会消失，因为导致出现滚动条的正是网页内容那点高度，这个问题还未解决；